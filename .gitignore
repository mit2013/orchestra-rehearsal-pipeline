# ---------------------------------------------------------------------------
# 録音素材そのもの(1ファイル 1〜2GB。リポジトリには入れない)
# ---------------------------------------------------------------------------
*.TAKE/
*.WAV
*.wav

# ---------------------------------------------------------------------------
# 生成物
#
# output/ 配下は原則すべて除外する(結合済みWAV・トリミング結果・プレビュー・
# 特徴量キャッシュ・波形画像はいずれも再生成できるうえ巨大)。
# ただし confirmed.json だけは「ユーザーが目視確認して確定した境界」という
# 手作業の成果であり、再生成できないので設定ファイルとして追跡する。
#
# 除外されたディレクトリの中身は再include できないため、
# output/ とその直下の日付ディレクトリは残したうえで中身だけを除外する。
# ---------------------------------------------------------------------------
/output/**
!/output/*/
!/output/*/confirmed.json
!/output/*/session_config.json

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# ---------------------------------------------------------------------------
# Claude Code のマシンローカル設定(絶対パスを含むため共有しない)
# ---------------------------------------------------------------------------
.claude/settings.local.json

# ---------------------------------------------------------------------------
# macOS
# ---------------------------------------------------------------------------
.DS_Store
