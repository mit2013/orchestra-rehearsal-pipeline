# アマオケ練習録音 自動編集パイプライン ― フェーズ1指示書

## 0. これは何か
オーケストラの練習録音(レコーダー: ZOOM M4 MicTrak、4ch/32bit float WAV、約3時間)を、編集〜MP3/WAV書き出し〜Google Drive/Boxへのアップロード〜Facebook Messengerでの共有まで自動化する、複数フェーズの取り組みの**フェーズ1**です。

フェーズ1のスコープは「取り込み → チャンネル結合 → 不要区間トリミング」まで。正規化・曲目単位エクスポート・クラウドアップロード・通知は次フェーズ以降で扱う。

最終目標: 手動で2〜3時間かかっている作業全体を10分程度に短縮すること。フェーズ1はその最初の検証ステップ。

## 1. 実行環境
- Mac上でローカル実行(Claude Code CLI)
- ffmpegはインストール済み(Homebrew)
- Python 3の仮想環境(venv)を作成し、必要なライブラリ(numpy, scipy, soundfile 等)をインストールしてよい
- 入力ファイルはM4からMacへコピー済みの状態からスタートする(コピー作業自体はフェーズ1の対象外)

## 2. 入力データ仕様
- 練習1回につき、複数の `日付_番号.TAKE` フォルダが存在する(例: `260802_001.TAKE`, `260802_002.TAKE`, ...)。番号はSDカードのファイルサイズ制限によるインクリメントで、時系列順。
- 各TAKEフォルダには以下3ファイルが入っている:
  - `{date}_{番号}_Tr1.WAV` — 外部マイク(Sony ECM-100U) CH1 = L、モノラル
  - `{date}_{番号}_Tr2.WAV` — 外部マイク CH2 = R、モノラル
  - `{date}_{番号}_TrMic.WAV` — M4内蔵マイク、ステレオ
- すべて32bit float WAV。サンプルレート等はTAKE間で共通のはず(念のため取り込み時に検証すること)。
- TAKE間の録音の隙間・ノイズは専用機のため発生しない(単純連結でよい。クロスフェード等は不要)。

## 3. フェーズ1のゴール
1. 全TAKEを走査し、日付・番号順にTr1/Tr2/TrMicが揃っていることを確認する
2. TAKEごとにTr1(L)+Tr2(R)を合成してステレオ化し、TAKE番号順に結合する → 外部マイクペアの1本の長尺ステレオWAV
3. TrMicをTAKE番号順に結合する → 内蔵マイクの1本の長尺ステレオWAV
4. 上記2本(外部マイク・内蔵マイク)に対し、不要区間(音出し・休憩・片付け)の候補を検出し、確認用の提案を生成する
5. ユーザーが提案を確認・修正した後、確定した区間で実際にトリミングを行い、トリミング済みの音声ファイル群を出力する

正規化・曲目単位でのエクスポート・タグ埋め込み・クラウドアップロード・Messenger通知は**フェーズ1では実装しない**。

## 4. 不要区間検出のヒント(重要)
練習の構造は概ね以下の繰り返し:

```
音出し → (合図) → チューニング → 合奏 → 休憩 → (合図) → チューニング → 合奏 → 休憩 → ...
```

無音検出だけでは判定できない。以下の音響的特徴を判定ロジックの手がかりにすること:

- **音出し・休憩中**: 各奏者がバラバラに個人練習しており、リズムの統一感がなく、ダイナミクス(強弱)のメリハリが乏しい。強音が途切れず連続する傾向があり、無音区間はむしろ少ない。
- **合奏中**: 指揮者が頻繁に演奏を止めて指示を出すため、無音・弱音区間が断続的に挟まる。全奏者が揃って演奏するため、音出し時間帯より音響的な統一感(タイミング・強弱の一致)が高い。
- **チューニング**: 単一の持続音(基準音)に近い、スペクトル的に単純な音が一定時間続く。音出し→合奏の切り替わり直前に必ず出現するため、区間境界の強力な手がかりになる。

これらを組み合わせた特徴量(例: 短時間窓ごとのRMSラウドネス、無音率、音量の分散/変化量、ピッチ安定性やスペクトルフラットネスなど)を使い、境界候補にスコアを付けて上位候補を提示する設計にすること。閾値だけに頼った単純な無音検出は避けること。

ユーザーは事前に「最終的に何分割にしたいか」のヒント(例: 前半/後半の2分割)を入力できるようにし、候補選定の参考にしてよい。

## 5. 確認ワークフロー
1. 候補区間をJSON形式で出力する(下記フォーマット参照)
2. 各境界候補の前後(例: ±15秒)を切り出した短いプレビュー音声を書き出す
3. 境界候補を可視化した波形画像(ffmpegの`showwavespic`等)を出力する
4. ユーザーがプレビュー音声・波形画像・JSONを見て、`action`(keep/remove)と`start`/`end`を必要に応じて手動編集する
5. 確定したJSONを読み込み、`action: keep`の区間のみを2系統(外部マイク/内蔵マイク)それぞれから切り出して出力する

### 候補JSONフォーマット例
```json
[
  {"index": 1, "start": "00:00:00", "end": "00:04:32", "label": "音出し(推定)", "action": "remove", "confidence": 0.8},
  {"index": 2, "start": "00:04:32", "end": "01:12:10", "label": "合奏1(推定)", "action": "keep", "confidence": 0.9},
  {"index": 3, "start": "01:12:10", "end": "01:22:00", "label": "休憩(推定)", "action": "remove", "confidence": 0.85},
  {"index": 4, "start": "01:22:00", "end": "02:35:00", "label": "合奏2(推定)", "action": "keep", "confidence": 0.9},
  {"index": 5, "start": "02:35:00", "end": "02:41:00", "label": "片付け(推定)", "action": "remove", "confidence": 0.7}
]
```

## 6. 出力ファイル・フォルダ構成(例)
```
output/{date}/
  raw_merged_ext.wav        # Tr1+Tr2結合、フル尺(トリミング前)
  raw_merged_int.wav        # TrMic結合、フル尺(トリミング前)
  candidates.json           # 境界候補の提案
  preview_clips/            # 境界前後のプレビュー音声
  waveform.png              # 境界マーク入り波形画像
  confirmed.json            # ユーザー確認・修正済みファイル
  trimmed/
    01_合奏1_ext.wav
    01_合奏1_int.wav
    02_合奏2_ext.wav
    02_合奏2_int.wav
```

## 7. 実装方針
- 音声の結合・トリミングは**ffmpeg**で行う(再エンコードを避け、可能な限り`-c copy`または明示的に`pcm_f32le`を指定して品質劣化を防ぐ)
- 特徴量抽出(ラウドネス・無音率・分散・ピッチ安定性等)は**Python**(numpy, scipy, soundfile等)で行う
- チャンネル結合(Tr1+Tr2→ステレオ)には`ffmpeg`の`amerge`または明示的なチャンネルマッピングを使用し、L/Rが正しい(Tr1=L, Tr2=R)ことをコード上で明示・検証すること
- 境界検出ロジックは、将来的に「合奏内の指揮者停止単位でのチャプター分割」や「指揮者発言のみカット」にも転用できるよう、汎用的な特徴量抽出モジュールとして疎結合に設計すること(フェーズ1では粗い区切りのみ使用するが、将来細かい粒度でも同じ仕組みを再利用する想定)
- WAVファイルは32bit float・数十分〜3時間分と大きいため、`view`やcat等でテキストとして直接開かないこと。メタデータ確認は`ffprobe`、特徴量解析は`soundfile`等のライブラリ経由で行い、バイナリの中身をそのままコンテキストに読み込まないこと

## 8. CLIコマンド(想定・調整可)
```bash
python pipeline.py ingest --date 260802
python pipeline.py merge --date 260802
python pipeline.py propose --date 260802 --splits 2
# ユーザーがプレビューを聞いて confirmed.json を編集
python pipeline.py apply --date 260802
```

## 9. ログ・エラーハンドリング
- 各TAKEでTr1/Tr2/TrMicの3点が揃っているか、サンプルレート・ビット深度が一致しているかを取り込み時に検証し、不整合があれば処理を止めて明示的にエラーを出す
- 3時間分の処理は時間がかかりうるため、進捗ログ(処理中のTAKE番号、経過時間等)を出力する
- 各ステップの中間ファイルは`output/{date}/`配下に残し、失敗時に途中から再開できるようにする

## 10. フェーズ1の範囲外(将来フェーズで検討)
- 音量正規化
- 曲目・楽章単位でのエクスポート(WAV/MP3)、MP3タグ埋め込み
- Google Drive / Boxへのアップロード、共有リンク発行(パスワード・ダウンロード制限含む)
- Facebook Messengerへの通知文言生成(送信自体はユーザーが手動で行う想定)
- 休憩・チューニングを挟まない楽章切り替えの検出
- 指揮者コメントの音声認識によるチャプターガイド作成
- 合奏内で指揮者が止めた単位でのより細かいチャプター分割
- 指揮者の発言をすべてカットし演奏部分のみを繋いだ「ダイジェスト版」音源の作成
- ノーマライズ以外の音質処理(EQ・コンプレッサー等)
