# アマオケ練習録音 自動編集パイプライン ― フェーズ3: Boxアップロード

## 0. 位置づけ
フェーズ2(曲目単位エクスポート・MP3タグ埋め込み)完了済み。本指示書は、`output/{date}/export/`配下のMP3を、Boxの日付フォルダにアップロードし、パスワード保護・ダウンロード不可の共有リンクを発行するところまでを実装する。

**スコープ外(別指示書)**:
- Google Driveへのアップロード(WAV、動画担当の動画担当向け)
- Facebook Messengerへの通知文言生成

## 1. 認証(OAuth 2.0)
- Box Developer Consoleで作成済みのアプリ(User Authentication / OAuth 2.0)を使う
- `.env`(gitignore対象)に以下を保存し、Claude Codeが読み込む:
  - `BOX_CLIENT_ID`
  - `BOX_CLIENT_SECRET`
- Redirect URIは`http://localhost:8888/callback`
- 初回認証フロー: ローカルにHTTPサーバーを起動 → Boxの認可URLをブラウザで開く → ユーザーがログイン・許可 → `http://localhost:8888/callback?code=...`にリダイレクトされる(このcodeは有効期限が短い(数十秒)ので、サーバーは事前に起動しておくこと) → codeをアクセストークン・リフレッシュトークンに交換する
- トークンはローカルファイル(例: `.box_tokens.json`、**gitignore対象**)に保存する
- **重要**: Boxのリフレッシュトークンは使用のたびに新しい値に入れ替わる(ローテーション)仕様。トークンをリフレッシュするたびに、新しいリフレッシュトークンを必ず保存し直すこと。これを怠ると、しばらくしてから認証が切れ、再度ブラウザでの許可が必要になる
- 正確なAPIのリクエスト・レスポンス形式は、Box公式ドキュメント(https://developer.box.com/guides/authentication/oauth2/)を参照して実装すること。本指示書の記載内容と食い違う場合は公式ドキュメントを優先する

## 2. 設定項目
`pipeline_defaults.json`に以下を追加する:
```json
{
  "orchestra": "Windrose Sinfonie Orchester",
  "concert_date": "",
  "box_parent_folder_id": ""
}
```
- `concert_date`: 演奏会本番日付(`yyyymmdd`形式)。**空のまま`box-upload`を実行しようとしたらエラーで停止すること**(パスワードが生成できないため)
- `box_parent_folder_id`: Box上の親フォルダID。空文字ならBoxのルート直下に日付フォルダを作る
- `ingest`時に`session_config.json`へコピーする(既存の`orchestra`と同じ扱い。日付ごとに個別上書き可能)

## 3. パスワードの生成規則
`{concert_date}{password_suffix}`(12桁、例: `{concert_date}{password_suffix}`)

## 4. フォルダの作成・再利用
- `box_parent_folder_id`(空ならルート)の直下に、`{date}`(例: `260802`)という名前のフォルダを探す
- 既に同名フォルダが存在する場合は、新規作成せずそれを再利用する(再実行時に重複フォルダができないように)

## 5. アップロード
- 対象: `output/{date}/export/*.mp3`のみ(WAVは対象外)
- **変更なしファイルはアップロードをスキップする**(Google Driveアップロードと同じ考え方): 同名ファイルが既に存在する場合、そのファイルの`sha1`をBox APIから取得し、ローカルファイルのSHA-1と比較する。一致すればアップロードせずスキップし、その旨をログに出力する。不一致の場合のみ、新しいバージョンとしてアップロードする(Boxのファイルバージョニング機能を使う)

## 6. 共有リンクの設定
- **フォルダ単位**で1つの共有リンクを設定する(ファイルごとではない)
- 設定内容:
  - パスワード保護: §3のパスワード
  - ダウンロード不可(プレビュー/ストリーミングのみ可能)
  - アクセス範囲: リンクを知っている人のみ(ログイン不要)
- 生成された共有リンクのURLを、実行結果として分かりやすく出力すること(次フェーズの通知文言作成で使う)

## 7. CLIコマンド
```bash
python pipeline.py box-upload --date 260802
```

## 8. 検証してほしいこと
- アップロード後、フォルダ内のファイル数が`export/`のmp3ファイル数と一致することを確認する
- 共有リンクの設定をAPI経由で読み戻し、ダウンロード不可・パスワード保護が有効になっていることを確認する(正確な確認方法・フィールド名はBox公式ドキュメントを参照すること。本指示書では断定しない)
- 実際に生成された共有リンクを(ブラウザで、あるいは可能なら別のAPI呼び出しで)開き、パスワード入力を求められること、ダウンロードボタンが出ない(プレビューのみ)ことを人間の目で確認できる形で報告すること
- `concert_date`が空の状態で実行した場合に、明確なエラーメッセージで停止することを確認する
- 260802で実行し、実際にBox上にフォルダとファイルができていることを確認する(260726は後で実行してよい)
