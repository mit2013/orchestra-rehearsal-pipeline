# アマオケ練習録音パイプライン ― 現場前処理と「帰宅前に MP3 配布を終える」

> **母艦側の実装と検証は完了している(§5)。** 現場での実測(§4-3)だけが未了で、
> 次の練習日に測る。当初案 §3-2 の「iPhone 側でゲインまで確定させ、母艦は
> `-c copy` で切るだけ」は採れないことが分かったので、§3-2 を書き直してある。

## 0. 位置づけ

コンセプトは**「帰り道にはもう聴ける」**である。練習が終わってから団員が聴けるように
なるまでの時間を、母艦(自宅の Mac)に素材を渡す前の段階で詰める。

達成する状態:

- **Box への MP3 配布と通知が、帰宅して母艦に素材を物理的に渡す前に完了している**
- Google Drive の WAV(動画担当向け)は帰宅後でよい

素材そのものは 3トラック約 8GB(32bit float)あり、モバイル回線で送れない。そこで
**iPhone 側で配布用の音を作ってしまい、小さくしてから母艦へ渡す**。

## 1. ブランチ

`main` から `feature/field-preprocess` を作成する。**`feature/audio-quality` が main に
マージされ、ラウドネス・コンプ・リミッターの設定値が確定してから切ること。**
iPhone 側と母艦側で設定が食い違うと、帰宅前に配った MP3 と帰宅後に作る WAV で
音が変わる。

## 2. 前提として確認済みの事実

- **ZOOM M4 は File Transfer(カードリーダー)モードを持つ。** コンピュータ・
  スマートフォン・タブレットから microSD の中身を読み書きできる。USB-C の iPhone
  なら直結、Lightning 機は Lightning - USB 3 カメラアダプタ経由。
  マニュアルは**M4 を電池で駆動すること**を求めている(スマートフォン接続時は
  USB からの給電では足りない)。出典: ZOOM M4 Operation Manual。
- **iPhone 上で ffmpeg を動かす手段がある。** a-Shell に ffmpeg が同梱されている。
- **素材のピークは 0 dBFS を超える**(260829 で +11.43 dBFS)。32bit float だから
  保持できている。**ゲインを下げる前に 16bit や非可逆圧縮に変換してはならない。**
  打楽器の一撃が全部クリップする。

## 3. 設計

### 3-1. 全体の流れ

```
[練習会場]
  M4 を File Transfer モードにし、USB-C で iPhone に接続(M4 は電池駆動)
  ↓
  ext の2トラック(Tr1 / Tr2)だけを対象にする。TrMic は現状の設定
  (source: ext_only)では使っていないので、現場では触らない
  ↓
  iPhone 上で「配布用プロキシ」を1本作る(§3-2)
      pipeline.py field-script が出す sh を a-Shell で流すだけ
  ↓
[移動中]
  プロキシを母艦へ送る(§3-4)
  ↓
  母艦: field-receive → propose → 境界レビューのページを公開
  ↓
  iPhone でレビューページを開き、頭と尻を聴いて判定・保存
  ↓
  母艦: field-export で確定境界に切りマスタリング、box-upload、notify
  ↓  ←←← ここまでが帰宅前
[帰宅後]
  microSD(または iPhone 内の原本)を母艦に渡す
  ↓
  母艦: ingest → merge → apply → normalize → mix → export
        32bit float の原本から WAV を書き出し、Drive へアップロード
```

### 3-2. iPhone 側で作るもの: 配布用プロキシ

**当初案は「ゲイン確定済みの配布用マスター」だった。これは採れない。**

当初案は、iPhone 側でラウドネス正規化・コンプ・リミッターまで当てて完成品を作り、
母艦は `ffmpeg -c copy` で MP3 フレーム境界に切るだけ、という設計だった。再符号化が
起きないので世代劣化がない。**ただしこれは、指示書を書いた時点の母艦が日単位の
ピーク正規化(`normalize_scope: date`)だったから成り立っていた。**
`feature/audio-quality` で母艦は**ブロックごとのラウドネス正規化**に変わっている。

260829 の実測(ext 系統、中央部で測った素の統合ラウドネス):

| ブロック | 統合ラウドネス | 実際に当てたゲイン |
|---|---|---|
| 合奏1 | -22.3 LUFS | +4.0 dB |
| 合奏2 | -13.4 LUFS | -5.7 dB |
| 合奏3 | -15.9 LUFS | -2.5 dB |

境界が決まる前に1本ぶんの単一ゲインを確定させると、**この 9.7 dB の差がそのまま
残る**。ブロック間で 8.6 LU ずれていたのが元の不満であり、そこへ戻ることになる。
「ブロックごとに測る」ためには境界が要り、境界は現場では決まっていない。

**そこでプロキシには、クリップを避けるための固定ゲインだけを載せる。**

- ステレオに組む(`ext_lr_map` が `swapped` の日は逆に組む)。式は母艦の
  `merge.JOIN_MAPS` をそのまま使う
- TAKE が複数ある日は `concat` で連結する。式は `merge.build_group_cmd` と同じ
- `volume=-14dB` を当てる。260829 の最大ピーク +11.43 dBFS に 2.6 dB の余裕がある
- 320kbps MP3 に符号化する。3時間で約 413MiB
- **1パスで済ませる。** 測ってから決めるのではなく固定値を当て、同じパスの中の
  `volumedetect` で結果を確認する。microSD からの読み出しが律速なので、2パス目を
  回すと所要が倍になる。ピークが -0.1 dBFS 以上ならログに警告が出る

ラウドネス正規化・コンプ・リミッターは、**境界が決まったあとに母艦がブロックごとに
当てる**。固定ゲインは可逆なので、母艦は測定の前に +14 dB を戻す。こうすると
プロキシ経由でも原本経由でも同じ目標ラウドネスに乗る(§5-1 で実測)。

代償は **MP3 の符号化が1世代増えること**である(`-c copy` では切れない)。この差が
実用上問題にならないことは §5-2 で確認した。

### 3-3. 母艦側: プロキシからブロックを切り出す

`pipeline.py field-export` が、`confirmed.json` の keep 区間ごとに次を行う。

1. プロキシの当該区間を +14 dB 戻して統合ラウドネスを測る
2. `loudness.solve_gain` で目標 -20 LUFS に乗るゲインを求める(`mix` と同じ手続き)
3. ゲイン → コンプ(2:1) → トゥルーピークリミッター(-1 dBTP、4倍オーバーサンプル)
   を通して 320kbps MP3 に書き出す
4. `export.write_tags` で ID3 タグを付ける

出力先は `output/{date}/export/` なので、`box-upload` がそのまま拾う。
`--variant` を付けると既配布分を差し替えずに別名で並べられる。

### 3-4. 転送手段

母艦は自宅にあり、iPhone は外にいる。どちらでもよいが、**母艦が受け取ったことを検知して
自動で処理を始められる**こと。

- iCloud Drive の所定フォルダに置き、母艦がそのフォルダを監視する
- Tailscale などで直接 `scp` する

母艦がスリープしていると始まらないので、練習日は起こしておく運用にするか、
Wake on LAN を使うこと。**この点はまだ決めていない。** 実装側は
`field-receive --input <パス>` でファイルを受け取るだけなので、どちらの方式でも
そのまま乗る。

### 3-5. 境界の確定

移動中に人が判断する必要がある。260829 で作った**境界レビューのページ**(各ブロックの
先頭25秒と末尾25秒を聴いて判定を入力し、保存するとページ自体が新しい版になる)を
そのまま使う。母艦が境界を提案したらこのページを公開し、iPhone で開いて判定する。

判定が保存されたら母艦が読み取り、確定として切り出しに進む。

## 4. 検証(必須)

### 4-1. 音の同一性

260829 の素材を使い、次の2つを比較する。→ **結果は §5-1 / §5-2**

- **A**: 母艦で 32bit float の原本から作った MP3(現行の経路)
- **B**: プロキシを作り、母艦で切り出した MP3(新しい経路)

### 4-2. 境界検出がプロキシでも同じ結果になること → **結果は §5-3**

### 4-3. 所要時間 ― **未了**

現場で M4 を接続してから、Box のアップロードと通知が終わるまでの実測時間を報告する。
内訳(吸い出し / iPhone での符号化 / 転送 / 母艦の解析 / 人の判定 / アップロード)を
分けて計測すること。どこが律速かが分かれば次の改善先が決まる。

母艦でプロキシを作ると 3時間ぶんで **4分11秒**だった(ローカル SSD からの読み出し)。iPhone の
a-Shell と microSD からの読み出しでどれだけ延びるかは、実際に測るまで分からない。
10分を大きく超えるようなら設計を見直す。

## 5. 実施結果(母艦側、260829)

検証はすべてスクラップディレクトリで行った(`--root` を切り替え、本番の
`output/260829/` は触っていない)。

### 5-1. ブロックごとのラウドネス ― 完全に一致した

プロキシ経由で求めたゲインは、原本経由のゲインと **0.01 dB まで一致した**。

| ブロック | 原本経由(normalize + mix) | プロキシ経由(field-export) |
|---|---|---|
| 前半 | +2.30 → +4.00 dB | +2.30 → +4.00 dB |
| 中盤 | -6.60 → -5.70 dB | -6.60 → -5.70 dB |
| 後半 | -4.10 → -2.50 dB | -4.10 → -2.50 dB |

配布物(MP3)を同じ条件で測った値も一致した。

| | A(原本経由) | B(プロキシ経由) |
|---|---|---|
| 前半 | -19.9 LUFS / 27.4 LU / -1.1 dBTP | -19.9 LUFS / 27.4 LU / -1.1 dBTP |
| 中盤 | -20.0 LUFS / 24.1 LU / -1.1 dBTP | -20.0 LUFS / 24.1 LU / -1.1 dBTP |
| 後半 | -20.3 LUFS / 23.9 LU / -0.8 dBTP | -20.3 LUFS / 23.9 LU / -0.8 dBTP |

**320kbps の符号化はラウドネス測定を動かさない。** プロキシに載せた固定ゲインは
測定前に打ち消しているので、ブロックごとの正規化はそのまま成立する。

なお後半の -0.8 dBTP は **両経路に共通**で、プロキシのせいではない。リミッター
出力の WAV は -1.1 dBTP に収まっており、MP3 に符号化する段で 0.3 dB 上がっている。
天井 -1.0 dBTP を 0.2 dB 超えるが、MP3 のデコードは浮動小数なのでクリップはしない。
気になるなら `mix --true-peak` を -1.3 程度にする。**これは現場経路とは別の話**。

### 5-2. 符号化が1世代増えることの影響 ― 聴感上は無視できる

同じ区間を両経路から取り出し、位置合わせしてから引き算した。

| 区間 | 位置ずれ | 相関 | 振幅差 | 残差(信号比) |
|---|---|---|---|---|
| 中盤 19:11-19:41 打楽器のクレッシェンド | 0.00 ms | 0.99976 | -0.003 dB | **-34.6 dB** |
| 前半 00:05-00:35 チューニング | 0.00 ms | 0.99993 | -0.000 dB | -39.2 dB |
| 後半 34:08-34:38 1楽章の頭 | 0.00 ms | 0.99992 | -0.001 dB | -38.9 dB |
| 後半 58:53-59:23 3楽章のティンパニ | 0.00 ms | 0.99992 | -0.001 dB | -39.2 dB |

**サンプル単位でずれない。** MP3 の符号化遅延は両経路で同じだけ入るので相殺される。
残差がいちばん大きいのは打楽器のクレッシェンドで -34.6 dB(過渡音は MP3 の苦手な
ところなので予想どおり)。320kbps の量子化雑音は元の信号にマスクされる帯域に入る
ため、この水準は聴き分けられない。**人の耳での確認は残っている。**

### 5-3. 境界検出 ― 提案は完全に一致した

同じコードを原本(`raw_merged_ext.wav`)とプロキシに当てた。

| | 原本 | プロキシ |
|---|---|---|
| 提案された区間 | 5 件 | 5 件、**すべて同一時刻** |
| チューニング検出 | 00:01:22(純度0.636) / 00:47:13(0.612) / 01:42:20(0.561) | 00:01:22(0.636) / 00:47:13(**0.611**) / 01:42:20(0.561) |
| チューニングの支配ピーク | 439.09 / 439.07 / 295.39 Hz | 439.09 / 439.07 / **295.38** Hz |
| 境界のスナップ | 01:42:15 | 01:42:15 |
| 判定しきい値 | 3.190 | 3.171(-0.6%) |
| 無音閾値 | -42.985 dBFS | **-56.984 dBFS** |

無音閾値の差 -13.999 dB は、プロキシに載せた固定ゲイン -14 dB そのものである。
閾値は素材から相対的に決まるので、ゲインを当てても判定は変わらない。

**MP3 の符号化で高域と微小レベルが変わることを心配していたが、20秒窓の特徴量には
出なかった。** 差はチューニングの純度で 0.001、支配ピークで 0.01 Hz である。

## 6. スコープ外

- TrMic(内蔵マイク)の現場での扱い。`source: ext_only` の運用が変わったら別途考える
- Drive への WAV アップロードを帰宅前に終えること。**これは帯域的に不可能**であり、
  目標に含めない
- iPhone 上での境界検出やダイジェスト生成。母艦にやらせる

### 5-4. 通しで走らせて分かったこと ― 律速は境界提案

プロキシを置き場に投入してから境界レビューのページが出るまで **25 秒**だった
(`field-watch`)。転送も符号化も律速ではない。**律速は `propose` の境界が当たらない
ことである。**

260829 の自動提案:

| | 提案 | 長さ |
|---|---|---|
| 合奏1(推定) | 00:00:00 – 01:34:36 | 94.6分。合奏1と休憩と合奏2をまたいでいる |
| 合奏2(推定) | 01:42:15 – 02:08:53 | 26.6分 |
| 合奏3(推定) | 02:50:23 – 03:00:24 | 10.0分 |

レビューページで秒単位に直せる幅を超えている。この日は実際に人が `confirmed.json` を
手で書き直していた。**帰宅前に配るという目的からすると、ここが通らなければ他が
どれだけ速くても意味がない。**

対策として `propose --tuning-first` を入れた。チューニングは合奏の直前に必ず現れ、
合奏の中には現れないので、これを合奏の開始とみなす。260829 では**3ブロックの開始が
すべて完全に一致した**(終了は guard のぶん +19〜+101 秒、削りすぎない側)。
既定にはしていない ― 検証できた日が1日しかない。

### 5-5. 現場経路のコマンド

```
field-script   現場で流す sh を出す(その日の TAKE 名を埋め込む)
field-watch    置き場を見張り、受け取り -> propose -> review-page まで
review-page    頭と尻のクリップを埋め込んだ判定ページを組み立てる
review-apply   ページに保存された判定を confirmed.json に反映する
field-export   確定境界で切り、ブロックごとにマスターして MP3 にする
```

転送方式が決まっていなくても `field-watch --dir` で通る。iCloud Drive でも
Tailscale の置き場でも「所定のフォルダにファイルが現れる」点は同じである。

## 7. 残っていること

- **§4-3 の現場実測。** 次の練習日に、M4 の接続から通知までを区間ごとに計測する
- **転送方式の決定(§3-4)。** iCloud Drive 監視か Tailscale か。実装は
  `field-watch --dir` でどちらにも乗るので、決めるのはフォルダの場所だけ
- **`--tuning-first` を既定にするかどうか。** 260829 では完璧だったが、検証できたのが
  1日だけ。次の練習日でもう一度確かめてから決める
- **人の耳での試聴(§5-2)。** 数値上は差がないが、打楽器のクレッシェンドを
  聴き比べておく
- 運用上の落とし穴の確認: M4 の電池の持ち、iPhone の空き容量(原本 8GB + プロキシ
  0.4GB)、母艦の起動
