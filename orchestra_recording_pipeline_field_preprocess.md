# アマオケ練習録音パイプライン ― 現場前処理と「帰宅前に MP3 配布を終える」

## 0. 位置づけ

コンセプトは**「帰り道にはもう聴ける」**である。練習が終わってから団員が聴けるように
なるまでの時間を、母艦(自宅の Mac)に素材を渡す前の段階で詰める。

達成する状態:

- **Box への MP3 配布と通知が、帰宅して母艦に素材を物理的に渡す前に完了している**
- Google Drive の WAV(動画担当向け)は帰宅後でよい

素材そのものは 3トラック約 8GB(32bit float)あり、モバイル回線で送れない。そこで
**iPhone 側で配布用の音を作ってしまい、小さくしてから母艦へ渡す**。

## 1. ブランチ

`main` から `feature/field-preprocess` を作成する。**`feature/audio-quality` が main に
マージされ、ラウドネス・コンプ・リミッターの設定値が確定してから切ること。**
iPhone 側と母艦側で設定が食い違うと、帰宅前に配った MP3 と帰宅後に作る WAV で
音が変わる。

## 2. 前提として確認済みの事実

- **ZOOM M4 は File Transfer(カードリーダー)モードを持つ。** コンピュータ・
  スマートフォン・タブレットから microSD の中身を読み書きできる。USB-C の iPhone
  なら直結、Lightning 機は Lightning - USB 3 カメラアダプタ経由。
  マニュアルは**M4 を電池で駆動すること**を求めている(スマートフォン接続時は
  USB からの給電では足りない)。出典: ZOOM M4 Operation Manual。
- **iPhone 上で ffmpeg を動かす手段がある。** a-Shell に ffmpeg が同梱されている。
- **素材のピークは 0 dBFS を超える**(260829 で +11.43 dBFS)。32bit float だから
  保持できている。**ゲインを下げる前に 16bit や非可逆圧縮に変換してはならない。**
  打楽器の一撃が全部クリップする。

## 3. 設計

### 3-1. 全体の流れ

```
[練習会場]
  M4 を File Transfer モードにし、USB-C で iPhone に接続(M4 は電池駆動)
  ↓
  ext の2トラック(Tr1 / Tr2)だけを対象にする。TrMic は現状の設定
  (source: ext_only)では使っていないので、現場では触らない
  ↓
  iPhone 上で「配布用マスター」を1本作る(§3-2)
  ↓
[移動中]
  配布用マスターを母艦へ送る(§3-4)
  ↓
  母艦: 解析 → 境界提案 → 境界レビューのページを公開
  ↓
  iPhone でレビューページを開き、頭と尻を聴いて判定・保存
  ↓
  母艦: 確定境界でブロックに切り分け、Box へアップロード、通知
  ↓  ←←← ここまでが帰宅前
[帰宅後]
  microSD(または iPhone 内の原本)を母艦に渡す
  ↓
  母艦: 32bit float の原本から WAV を書き出し、Drive へアップロード
```

### 3-2. iPhone 側で作るもの: 配布用マスター

**全長 1本の、ゲイン確定済み 320kbps MP3(ステレオ)。** ブロックには切らない。
境界はまだ決まっていないためである。

処理は ffmpeg の2パスで行う。

1. Tr1 を L、Tr2 を R に組んでステレオ化する(`ext_lr_map` が `swapped` の日は逆に組む)
2. 1パス目: ラウドネスを測定する
3. 2パス目: 測定値をもとにゲインを当て、コンプとリミッターを通し、320kbps MP3 に符号化

**ゲイン・コンプ・リミッターの設定は、母艦側の実装と完全に一致させること。**
`orchestra_recording_pipeline_loudness.md` で決める値をそのまま使う。ここが食い違うと、
帰宅前に配った MP3 と帰宅後に作る WAV で音が変わってしまう。**この指示書の実装は
ラウドネス正規化の指示書の完了を前提とする。**

3時間ぶんで約 400MB になる。

### 3-3. 母艦側: 配布用マスターからブロックを切り出す

**再符号化しないこと。** `ffmpeg -ss ... -to ... -c copy` で MP3 のフレーム境界で切る。
境界の精度は ±26ms 程度になるが、この用途では十分である(境界は秒単位で決めている)。
再符号化すると世代劣化する。

ID3 タグは切り出したあとに付与する。既存の `export.py` のタグ付けをそのまま使えるはず。

### 3-4. 転送手段

母艦は自宅にあり、iPhone は外にいる。どちらでもよいが、**母艦が受け取ったことを検知して
自動で処理を始められる**こと。

- iCloud Drive の所定フォルダに置き、母艦がそのフォルダを監視する
- Tailscale などで直接 `scp` する

母艦がスリープしていると始まらないので、練習日は起こしておく運用にするか、
Wake on LAN を使うこと。この点は実装前に方式を決めること。

### 3-5. 境界の確定

移動中に人が判断する必要がある。260829 で作った**境界レビューのページ**(各ブロックの
先頭25秒と末尾25秒を聴いて判定を入力し、保存するとページ自体が新しい版になる)を
そのまま使う。母艦が境界を提案したらこのページを公開し、iPhone で開いて判定する。

判定が保存されたら母艦が読み取り、確定として切り出しに進む。

## 4. 検証(必須)

### 4-1. 音の同一性

260829 の素材を使い、次の2つを比較する。

- **A**: 母艦で 32bit float の原本から作った MP3(現行の経路)
- **B**: iPhone で配布用マスターを作り、母艦で切り出した MP3(新しい経路)

比較項目:

- 統合ラウドネス、トゥルーピーク、ラウドネスレンジが実質的に一致すること
- **打楽器のクレッシェンド(中盤 19:11–19:41)を聴き比べ、差が分からないこと。**
  ここが一番クリップしやすい
- チューニング(各ブロック冒頭)が同じ音量で聞こえること

### 4-2. 境界検出がプロキシでも同じ結果になること

現行の `propose` は `raw_merged_ext.wav`(非圧縮)に対して動く。新しい経路では MP3 を
デコードしたものに対して動くことになる。**同じ境界が出ることを確認すること。**

260829 で、原本由来と MP3 由来のそれぞれに `propose` をかけ、提案される境界と
チューニング検出の結果を突き合わせる。差が数秒を超える場合は、特徴量のどこが
変わったのかを報告すること(MP3 の符号化で高域と微小レベルが変わるため、
無音閾値やスペクトル平坦度に影響しうる)。

**この検証はスクラップディレクトリで行うこと。本番の `output/260829/` を対象にしない。**

### 4-3. 所要時間

現場で M4 を接続してから、Box のアップロードと通知が終わるまでの実測時間を報告する。
内訳(吸い出し / iPhone での符号化 / 転送 / 母艦の解析 / 人の判定 / アップロード)を
分けて計測すること。どこが律速かが分かれば次の改善先が決まる。

iPhone 側の符号化が現実的な時間で終わるかは、実際に測るまで分からない。3時間ぶんの
2パス処理が10分を大きく超えるようなら、この設計自体を見直す必要がある。

## 5. スコープ外

- TrMic(内蔵マイク)の現場での扱い。`source: ext_only` の運用が変わったら別途考える
- Drive への WAV アップロードを帰宅前に終えること。**これは帯域的に不可能**であり、
  目標に含めない
- iPhone 上での境界検出やダイジェスト生成。母艦にやらせる

## 6. 完了後の報告

- 4-1 の比較結果(数値と試聴)
- 4-2 の境界の一致状況
- 4-3 の所要時間の内訳と律速箇所
- 運用上の落とし穴(M4 の電池の持ち、iPhone の空き容量、母艦の起動など)
