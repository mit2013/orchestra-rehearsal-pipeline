# アマオケ練習録音パイプライン ― ラウドネス正規化と緩いコンプレッション

## 0. 位置づけ

打楽器が入った 260829 で、現行のピーク正規化の限界がはっきり出た。ブロック間で
**8.6 LU** の音量差が生じ、ダイナミックレンジは 24〜28 LU に達している。
`orchpipe/normalize.py` と `orchpipe/mix.py` を、ラウドネス基準に置き換える。

## 1. ブランチ

`main` から `feature/loudness-normalize` を作成する。

## 2. 現状の実測(260829、配布したファイル)

| | 統合ラウドネス | ラウドネスレンジ | トゥルーピーク |
|---|---|---|---|
| 前半(合奏1) | **−34.3 LUFS** | 27.7 LU | **−10.3 dBFS** |
| 中盤(合奏2) | −25.7 LUFS | 24.5 LU | −1.1 dBFS |
| 後半(合奏3) | −28.5 LUFS | 24.5 LU | −1.0 dBFS |

### 問題1: ブロック間で 8.6 LU ずれている

`session_config.json` の `normalize_scope: "date"` は、日付全体でいちばん大きいピークを
基準に**全ブロック共通のゲイン**を決める。260829 では合奏3のピーク +11.43 dBFS が基準に
なり、全ブロックに −12.43 dB がかかった。合奏1のピークは +0.31 dBFS しかなく、必要の
ない 12 dB 近くまで一緒に下げられている。

**打楽器が鳴ったブロックが基準を決め、鳴らなかったブロックが道連れになる。**
前半から中盤へ再生を移すと、聴く側は音量つまみを触ることになる。

### 問題2: ダイナミックレンジが広すぎる

24〜28 LU は、放送のクラシック番組(15〜20 LU 程度)より広い。スマートフォンや
車内で聴く用途には向かない。チューニングが −45〜−50 dBFS まで沈むのはこれが理由である。

なお**素材のピークは 0 dBFS を超えている**(+11.43 dBFS)。32bit float だから保持できて
いるだけで、ゲインを下げる前に 16bit や非可逆圧縮に落とすと打楽器の一撃がクリップする。
処理順序の制約としてどこでも効いてくる。

## 3. 直すこと

### 3-1. ピーク正規化をラウドネス正規化に置き換える

各ブロックを**統合ラウドネスの目標値**に合わせる。目標は **−20 LUFS** を既定とし、
引数(`--target-lufs`)で変更できるようにする。練習録音は音楽配信(−14 LUFS 前後)ほど
上げる必要はなく、下げすぎると現状の不満が残る、という中間を取った値である。実際の
値は §5 の試聴で決めてよい。

測定と適用は ffmpeg の `loudnorm` フィルタを **2パス**で使う(1パス目で測定、2パス目で
測定値を渡して適用)。1パス目の測定結果は `analysis.json` 相当の場所に残し、再実行で
同じ結果になるようにすること。

これにより `normalize_scope`(`date` / `block`)の区別は不要になる。ブロックごとに
目標ラウドネスへ合わせるので、ブロック間の音量は自動的に揃う。設定キー自体を廃止するか、
互換のため残して無視するかは実装者の判断でよいが、README に明記すること。

### 3-2. 緩いコンプレッションを加える

**不自然にならない範囲**であることが要件である。打楽器の一撃だけを抑え、弦のフレーズの
表情には触らないこと。出発点として次の値を提案する。

| パラメータ | 値 | 意図 |
|---|---|---|
| レシオ | 2:1 | 明確に「かかっている」と分からない程度 |
| しきい値 | 統合ラウドネスの +6 dB 程度 | 平均的な合奏には触らない |
| アタック | 100 ms 前後 | 打楽器の立ち上がりの質感を残す |
| リリース | 1 秒前後 | フレーズ単位で戻る。ポンピングを避ける |
| ニー | ソフト | しきい値付近の不連続をなくす |

### 3-3. 処理順序

**ゲイン → コンプレッサ → トゥルーピークリミッター(−1 dBTP)** の順に当てる。

コンプで打楽器の立ち上がりを先にならしておけば、リミッターはほとんど動かずに済む。
リミッターを先に置くと、打楽器のピークだけがリミッターに当たって音色が変わる。

リミッターは安全弁であり、常時動作させるものではない。§5 でリミッターの動作量
(ゲインリダクションの最大値と総時間)を測り、報告すること。

## 4. 適用範囲

新しく取り込む日付に適用する。**既に配布済みの 260726・260802・260829 を作り直すかどうかは
別途判断する。** 音量が変わるため、団員が既に持っているファイルと差が出る。実装と検証が
終わった時点で、新旧の比較材料(同じ箇所の聴き比べ)を用意して判断を仰ぐこと。

## 5. 検証(必須)

### 5-1. 数値

260829 の3ブロックで:

- 統合ラウドネスが目標値の **±1 LU 以内**に揃うこと(ブロック間の差が 1 LU 以下)
- トゥルーピークが **−1 dBTP を超えないこと**
- ラウドネスレンジが **15〜18 LU 程度**まで縮まること。10 LU を下回るようなら圧縮が
  強すぎる。オーケストラの弱奏の意味が失われる

260726・260802 でも同じ測定を行い、打楽器のない日で不自然な持ち上がりが起きないことを
確認する。

### 5-2. 試聴(必須)

以下の3箇所を、処理前後で聴き比べること。

| 箇所 | 何を確かめるか |
|---|---|
| 260829 中盤 19:11–19:41(打楽器のクレッシェンド) | 頭が持ち上がりすぎて不自然になっていないか。ポンピングがないか |
| 260829 前半の冒頭(チューニング) | 小さすぎて聞こえない状態が解消されているか |
| 260829 後半のトゥッティ | 打楽器の一撃が潰れて質感を失っていないか |

**この試聴が最終的な合否判定である。** 数値が目標に収まっていても、音が不自然なら
パラメータを見直すこと。

### 5-3. 既存処理への非影響

トリミング境界(`confirmed.json`)、曲目単位のエクスポート、アップロード、通知の
挙動が変わらないこと。変わるのは音量とダイナミクスだけである。

## 6. スコープ外

- EQ、ノイズ除去、リバーブなど音色に関わる処理。今回は音量とダイナミクスのみ
- ブロック内での自動的な音量追従(オートゲイン)。フレーズごとに音量が動くのは
  練習録音として不自然になる

## 7. 完了後の報告

- 3日ぶん(260726・260802・260829)の処理前後のラウドネス測定値
- リミッターのゲインリダクション量
- 5-2 の試聴結果
- 既配布分を作り直すかどうかの判断材料(聴き比べ用の抜粋)
