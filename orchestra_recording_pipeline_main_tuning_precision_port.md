# アマオケ練習録音パイプライン ― mainへの還元: チューニング検出精度の統合

## 0. 位置づけ
研究ブランチ(`research/fine-segmentation`)で実データにより検証された**チューニング検出の
改良のみ**を、mainへ統合する。4値分類・ダイジェスト・書き起こし等、研究ブランチの他の
機能は今回は移植しない(まだ実用の精度に達していないと判断したため)。

## 1. ブランチ
`main`から新しいブランチ`feature/tuning-precision`を作成すること。
`research/fine-segmentation`とは別系統として扱う(そちらは今後も並行して開発が続く)。

## 2. 移植するロジック
research/fine-segmentationでの実装・実測(5/5・最大誤差0.52秒)を踏まえ、mainの
チューニング検出を以下のように置き換える:

- 基準音A(既存の想定値、約442Hz)の**倍音1〜5次まで**、±60cent程度の許容幅で一致を見る
  (オクターブのみを見ていた現行実装を置き換える。静かなオーボエ単独音は基音より上位倍音の
  方が支配的になることがあるため)
- **ピッチ安定性の条件は外す**(ピークが基音・倍音間を行き来すること自体が正常な挙動で
  あり、これを「不安定」として除外すると本物のチューニングを取り逃がすため)
- 候補区間の採用は**「純度」**(区間内で条件を満たすフレームの割合)で選ぶ。あわせて
  **区間長12秒以上**を必須とする
- 検出はトリミング済み(`trimmed/`)ではなく、**TAKE結合後の連続ファイル**に対して行う
  (真のチューニングがトリミング境界の前後どちらに位置しても検出できるようにするため)

## 3. 境界(guard)ロジックの見直し
現行の「音出し→合奏」境界は、粗いViterbi探索の結果に固定のguard(120秒)を足したもので、
実際のチューニング開始との関係が日付・ブロックによって不統一(260802合奏2は切り落とし、
他4ブロックは79〜128秒の余分)であることが研究ブランチで判明している。

新しい境界は、上記2で検出したチューニング開始時刻から**数秒(3〜5秒を目安に、妥当な値を
選んでよい)手前**を基準とする。これにより、粗いguardに頼らず、日付・ブロックによらず
一貫した基準にする。

## 4. 検証(必須)
以下5点の正解データ(人手で確認済み)に対し、新しい境界が**すべて「実際のチューニング
開始と同じか、それより手前」**(演奏・チューニングを絶対に削らない)であることを確認する:

| データセット | ブロック | チューニング開始(正解) |
|---|---|---|
| 260802 | 合奏1 | 00:10:01 |
| 260802 | 合奏2 | 01:40:43 |
| 260726 | 合奏1 | 00:08:37 |
| 260726 | 合奏2 | 01:14:31 |
| 260726 | 合奏3 | 02:24:49 |

あわせて、既存の`confirmed.json`との差分(境界がどれだけ動いたか)を全5ブロックについて
報告すること。特に260802合奏2は、境界が`01:41:54`から前方向へ大きく動くはずなので、
その差分を明示すること。

既存のリグレッションチェック(sha256等)の手法を踏襲し、この変更が他の処理(正規化・
ミックス・エクスポート・アップロード)に意図せず影響していないことも確認すること。

## 5. スコープ外(この指示書では行わない、例外的にYes/No確認してよい)
- 研究ブランチの4値分類・ダイジェスト・書き起こし機能のmainへの統合
- **既に配布済みの260802・260726のWAV/MP3の再生成・再アップロード・再通知**。
  実装・検証が完了した時点で結果を報告し、再配布するかどうかは人間の判断を仰ぐこと
  (これは外部の第三者(動画担当・団員)へ既に届いているファイルを差し替えるかどうかの
  判断であり、この指示書全体の「Yes/No確認をしない」方針の例外として扱う)

## 6. 完了後の報告
- 5点の検証結果(すべて安全側に収まっているか)
- `confirmed.json`との差分一覧
- 既存のリグレッションチェックで他の処理に影響がないことの確認
