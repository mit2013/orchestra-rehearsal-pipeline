# アマオケ練習録音 自動編集パイプライン ― フェーズ4: Google Driveアップロード

## 0. 位置づけ
フェーズ3(Boxアップロード)と並行する形で実装する。Google Driveへは、動画担当の動画担当向けにWAVをアップロードする。BoxのMP3(団員向け、パスワード・ダウンロード不可)とは性格が異なり、こちらは動画編集用の素材としての受け渡しなので、**ダウンロード制限は付けない**。

**スコープ外(別指示書)**: Facebook Messengerへの通知文言生成。

## 1. 認証(OAuth 2.0)
- `.env`(gitignore対象)に以下を保存済み:
  - `GOOGLE_CLIENT_ID`
  - `GOOGLE_CLIENT_SECRET`
- Pythonの`google-auth-oauthlib`(`InstalledAppFlow`)を使う。デスクトップアプリ用のクライアントなので、Redirect URIの事前登録は不要
- 初回認証: `run_local_server(port=0)`(空いているポートを自動選択)でローカルサーバーを起動し、ブラウザでの認可を待つ → 完了後、トークンをローカルファイル(`.google_tokens.json`、**gitignore対象**)に保存する
- **Boxとの違いに注意**: Googleのリフレッシュトークンは通常ローテーションしない(同じ値を使い続けられる)。ただし、リフレッシュ時にアクセストークンを更新した場合は都度保存すること
- OAuth同意画面は既に「本番環境」ステータスに変更済み(テストのまま7日で失効する問題への対処は完了している)

## 2. スコープ
`https://www.googleapis.com/auth/drive.file`(このアプリが作成したファイルのみにアクセスする限定的なスコープ)

## 3. フォルダ構成
- Google Driveのルート直下に「練習録音」という名前のフォルダを探す。存在すれば再利用し、無ければ新規作成する
- その「練習録音」フォルダの中に、日付名(例: `260802`)のフォルダを探す。存在すれば再利用し、無ければ新規作成する(再実行時に重複フォルダを作らないため)

## 4. アップロード対象とファイル名
- 対象: `output/{date}/trimmed/{idx}_合奏{idx}_final.wav`(**ローカルにコピーを作らず、この既存ファイルを直接アップロード元とする**。`export/`はMP3専用なのでこのフェーズでは触らない)
- アップロード先での表示名(Drive上のファイル名): `{date}_{タイトル}.wav`(タイトルはフェーズ2と同じ採番ルール: 2ブロック→前半/後半、3ブロック→前半/中盤/後半、それ以外→コマ1/コマ2...)
- ローカルの`trimmed/`配下は読み取り専用として扱い、変更・複製しないこと
- 同名ファイルが既にDrive上に存在する場合(再実行時): エラーにせず、新しいバージョンとしてアップロードする

## 5. 共有設定
- 「リンクを知っている全員が閲覧者」(`type: anyone, role: reader`)
- **Boxと異なり、ダウンロードを禁止しないこと**(動画担当がファイルを取得して編集に使うため、ダウンロード可能である必要がある)
- フォルダ単位で共有リンクを1つ設定する(ファイルごとではない)
- 生成された共有リンクのURLを、実行結果として分かりやすく出力すること(次フェーズの通知文言作成で使う)

## 6. CLIコマンド
```bash
python pipeline.py gdrive-upload --date 260802
```

## 7. 検証してほしいこと
- アップロード後のファイル数が、その日付の合奏ブロック数と一致すること
- 「練習録音」フォルダ・日付フォルダが、再実行しても重複作成されないこと(既存フォルダを再利用すること)
- 共有権限を読み戻し、「リンクを知っている全員が閲覧者」になっていることを確認すること
- ダウンロードが禁止されていない(Boxと違う)ことを確認すること
- 260802で実行し、実際にファイルがアップロードされ、共有リンクが機能する(ブラウザで開いてダウンロードできる)ことを確認すること
