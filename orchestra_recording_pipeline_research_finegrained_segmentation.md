# アマオケ練習録音 パイプライン ― 研究ブランチ: 音源分析の高度化

## 0. 位置づけ・運用方針

本指示書は`research/fine-segmentation`ブランチ上でのみ作業する試作(プロトタイプ)。
`main`ブランチの既存パイプライン(フェーズ1〜5)には一切影響を与えないこと。新規コードは
`orchpipe/research/`配下に置き、既存モジュール(`features.py`等)は**インポートして再利用
してよいが、変更はしない**(mainとの互換性を保つため)。

**運用方針(重要)**: この指示書の範囲内であれば、Yes/Noの確認を都度求めず、最後まで
実装・検証・レポート作成まで進めてください。判断に迷う分岐がある場合は、各ステージの
「迷ったらこうする」に従うこと。それでも判断できない・仕様が根本的に成立しない場合のみ、
作業を中断せず**その時点までの成果とレポートをまとめた上で**、レポートの中で明示的に
質問・提案を書くこと(会話を止めて質問する、ではなく、成果物として問いを残す)。

**例外**: ステージB-0(正解データの人手取得)およびステージF-4(状態分類の精度評価用
シートの記入待ち)は、判断の確認ではなく「まだ存在しないデータ」を人間から受け取る
工程なので、ここでは実際に立ち止まって入力を待ってよい。それ以外の箇所では
立ち止まらないこと。

各ステージの完了時に、意味のあるまとまりとしてコミットすること(mainと同様の運用)。

**依存関係**: ステージA(4値分類)が土台。B・Cはそこから派生、D→Eの順で依存する。

```
A(状態分類) ─┬─→ B(合奏開始精度)
             ├─→ C(ダイジェスト版)
             └─→ D(文字起こし) ─→ E(楽章切れ目推定)
```

---

## ステージA: 細分化ステート分類(無音 / チューニング / 発言 / 演奏)

### A-1. 対象と粒度
既存の`trimmed/`(合奏ブロック単位、トリミング済み)を対象に、フレーム単位(0.5秒目安の
ホップ幅、既存`features.py`の`aggregate_windows`を細粒度で呼び出せるよう拡張してよい)で
以下4クラスのいずれかにラベル付けする:

- `silence`(無音)
- `tuning`(チューニング)
- `speech`(指揮者または他の人が喋っている)
- `playing`(オーケストラが演奏している)

### A-2. 判定方法
- `silence`: 既存の無音判定ロジック(RMS閾値)を流用
- `tuning`: 既存のチューニング検出を改良する。README(`report.md`)に記載された既知の
  改善案 ― 「検出対象をA≈440–443Hzとそのオクターブ付近に限定し、かつ直後に音量が
  落ちることを条件に加える」― をこの機会に実装すること
- `speech` / `playing`: 独自の音声分類器を新規に作らない。ステージDで使う音声認識
  (Whisper系、詳細はD参照)のVAD(発話区間検出)結果を流用する。VADが発話ありと
  判定し、かつ書き起こし結果が一定の確信度・妥当性(繰り返しトークンや空文字でない)を
  満たす区間を`speech`、それ以外の非無音区間を`playing`とする
- 処理コストを抑えるため、まず`features.py`の既存特徴量(スペクトルフラットネス等)で
  「音楽的に単純・チューニングでもない」候補区間を粗く絞り込んでから、その候補区間にのみ
  音声認識を適用してよい(3時間分全体に無条件でVADをかける必要はない)。**この最適化は
  必須ではなく、実行時間が問題になった場合にのみ行うこと**(迷ったら: まず全区間に
  適用し、遅すぎれば絞り込みを追加する)

### A-3. 出力
`output/{date}/research/{idx}_states.json`: 区間のリスト(開始・終了・ラベル・確信度)

### A-4. 検証
- 260802・260726それぞれの`trimmed/`全ブロックに適用する
- 各ラベルの合計時間・出現回数を集計し、レポートに記載する(正解データがないため、
  定量的な正解率ではなく、既知の事実(例: 各合奏の冒頭に必ずチューニングがあるはず)との
  整合性を確認する形でよい)
- 迷ったら: 4クラスの閾値・パラメータは、この2データセットで明らかに不合理な結果
  (例: 演奏中のはずの区間が大半speechになる等)が出ない範囲で、常識的な値を選んで進める。
  最適化はステージB以降の結果を見てから

---

## ステージB: 合奏開始検出の精度向上

### B-0. 正解データの再取得(人手の聴き取りが必要、自動化しない)
以前渡した基準時刻(音出し→合奏1の`confirmed.json`境界)は、トリミング用の安全側マージン
(guard)込みの値であり、「演奏を削らない」ことだけを基準に検証されたものである。
**チューニング開始のピンポイントな秒数としての精度は検証されていない**ため、B-1以降の
基準としてそのまま使わないこと。

最初に以下を行い、Claude Codeはここで一度止まって人間の確認を待つこと(この待ち合わせは
「判断」ではなく「存在しないデータの取得」なので、運用方針の「Yes/No確認をしない」の
例外として扱う):

1. 下記の概算時刻(±30秒程度の前後を含む)を中心に、5箇所それぞれの短い(1分程度)
   プレビュークリップを`output/{date}/research/tuning_check/`に書き出す
2. 概算時刻(参考値、精度は保証しない):

   | データセット | ブロック | 概算時刻(参考) |
   |---|---|---|
   | 260802 | 合奏1 | 00:08:42 |
   | 260802 | 合奏2 | 01:41:54 |
   | 260726 | 合奏1 | 00:08:16 |
   | 260726 | 合奏2 | 01:14:28 |
   | 260726 | 合奏3 | 02:25:12 |

3. クリップを書き出したら、そこで人間からの正確な秒数の入力を待つこと

### B-1. 目標
「聴き始めて3秒後にチューニング開始」レベルまで、合奏開始(音出し→チューニング→合奏の
チューニング開始時刻)の検出精度を高める。

### B-2. 正解データ
B-0で人間が確定させた5点の正確な秒数を基準とすること(B-0以前の概算値は使わない)。

### B-3. 実装方針
ステージAで細分化した`tuning`区間のうち、各合奏ブロック開始直前にあるものの**開始時刻**
(オンセット)を、フレーム単位の細かい時間分解能で特定する。既存の粗いViterbi境界とは
別に、この細粒度データから独立してオンセットを求めること。

### B-4. 受け入れ基準
上記5点全てで、検出値と真の開始時刻の差が±3秒以内。達成できない場合は、5点それぞれの
誤差をレポートに記載し、原因の仮説(既存の`report.md`にある「本物のチューニングを
取り逃がして合奏内で誤検出している」等)を添えること。ここで作業を止めず、達成できた
範囲でステージC・Dへ進んでよい。

---

## ステージC: ダイジェスト版の作成

### C-1. 処理内容
ステージAで`playing`と判定された区間を、前後3秒程度のマージンを付けて抽出し、
時系列順に結合する。マージン付与により隣接区間と重なる場合は重複させず1つに統合する。
結合点には短いクロスフェード(50〜100ms程度)を入れ、クリックノイズを避けること。

### C-2. 出力
`output/{date}/research/{idx}_digest.wav`(32bit float、既存の`_final.wav`と同じ
チャンネル構成)

### C-3. 検証
- 260802・260726で生成し、元の合奏ブロック尺に対してダイジェスト版がどれだけ短縮
  されたか(圧縮率)をレポートに記載する
- 結合点でクリックノイズが出ていないか、波形上でピークの不連続がないか確認する
- 迷ったら: マージンで演奏を削りすぎるより多めに残す(これまでの安全側マージンの
  方針を踏襲)

---

## ステージD: 指揮者発言の抽出と文字起こし

### D-1. 使用ツール
ローカルで動く音声認識(faster-whisper推奨、日本語対応・CPUでも実用速度)を使う。
モデルサイズは`medium`を既定とし、実行時間が長すぎる場合は`small`に落としてよい
(迷ったら: 精度を優先し`medium`から始める)。新規の依存関係として追加してよい。

### D-2. 処理内容
ステージAで`speech`と判定された区間を対象に書き起こしを行う。タイムスタンプ付きで
出力すること。

### D-3. 出力
`output/{date}/research/{idx}_transcript.json`(開始・終了・書き起こしテキストの配列)、
および人が読みやすい`.txt`版も併せて出力する。

### D-4. 検証
- 生成された書き起こしを実際に目視確認し、明らかな破綻(同じ語の無限リピート等、
  音楽を誤って書き起こしにかけた兆候)がないかレポートに記載する
- **音質(近接マイクではなく部屋鳴りのマイクで拾った声)による精度の限界は、試作段階の
  既知の制約として正直に記載すること**。完璧な書き起こしを目指すのではなく、
  ステージEで使える程度の実用精度を目指す

---

## ステージE: 曲目・楽章の切れ目推定

### E-1. スコープ(重要)
今回は**書き起こしテキストからのキーワード検出のみ**を実装する。演奏の音楽的な
特徴変化(曲想・調性・テンポの変化)からの推定は、今回のスコープ外とし、レポートの
「今後の課題」に回すこと。

### E-2. 実装方針
ステージDの書き起こしに対し、楽章・曲目の切れ目を示唆する語彙パターンを検出する
(正規表現・キーワードマッチングでよい)。例:
- 「第◯楽章」「◯楽章」(◯は漢数字・算用数字)
- 「次は」「次、」に続く曲名・楽章表現
- 「頭から」「もう一度」等の再開を示す表現

これは提案(候補)であり、断定しないこと。各候補に、検出根拠となった書き起こしの
該当テキストを添えて出力する。

### E-3. 出力
`output/{date}/research/{idx}_section_candidates.json`(時刻・候補理由・該当テキスト)

### E-4. 検証
- 260802・260726で実行し、検出された候補数と、その根拠テキストをレポートに列挙する
- 正解データがないため、精度の数値評価はしない。候補が明らかに無意味(該当テキストが
  切れ目と無関係)でないかを目視確認する

---

## 最終レポート

全ステージ完了後、`output/research_report.md`として以下をまとめること:

1. 各ステージの実施結果・受け入れ基準の達成状況
2. 迷ったが独断で進めた判断とその理由の一覧
3. 未達成の目標とその原因仮説
4. 次に検討すべきこと(音楽的特徴変化による楽章推定を含む)
5. 処理時間(3時間分の音声を通した場合の概算)

レポート完成をもって、このセッションでの作業完了とすること。

---

## ステージF(追加): 状態分類(silence/speech/playing)の精度評価(パイロット)

チューニングはB-0で人間の正解データがあるが、silence/speech/playingは既知の事実との
整合性チェックに留まっている。全区間の目視確認は非現実的なため、サンプリングと視覚化で
評価する。**まず260802合奏2のみを対象にしたパイロットとする**(他ブロックへの展開は
この結果を見てから判断する)。

### F-1. レビュー対象の抽出(優先順位順)
`states.json`から以下を抽出する:
1. `speech`と判定された区間 ― 全数
2. 5秒以上の`silence`区間 ― 全数
3. クラスが切り替わる境界(前後5秒を含む) ― 全数(誤分類は境界に集中しやすいため)
4. `playing`区間の層化サンプリング ― 全体を10分ごとに分け、各区間から2〜3件ランダムに
   選ぶ(目安20〜30件)

### F-2. 視覚的な一覧の作成
対象ブロックを10分ごとに分割し、各区間について波形+スペクトログラムに予測ラベルを
色分けした帯を重ねた画像を出力する。人間が最初に目視でざっと見て、明らかな誤りの当たりを
付けられるようにする。

### F-3. レビュー用クリップとシートの作成
- F-1で抽出した各候補について、`output/{date}/research/eval/{block}/clips/`に短い
  音声クリップを書き出す(ファイル名は連番+時刻+予測ラベル)
- `output/{date}/research/eval/{block}/review_sheet.csv`に、クリップID・開始・終了・
  予測ラベル・正解ラベル(空欄)・メモ(空欄)の列を持つCSVを出力する

### F-4. ここで停止する(例外)
F-2・F-3の生成物ができたら、そこで作業を止め、人間が画像を見てCSVに正解ラベルを
記入するのを待つこと。これはB-0と同じ理由(判断ではなくデータ取得)による例外である。

### F-5. 記入後の集計
CSVへの記入が確認できたら、クラスごとの適合率・再現率・混同行列を計算し、
`research_report.md`に追記する。誤分類のパターンが見つかれば、改善案を提案すること。
