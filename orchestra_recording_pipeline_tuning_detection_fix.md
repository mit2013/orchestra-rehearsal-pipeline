# アマオケ練習録音パイプライン ― チューニング検出の誤検出をなくす

## 0. 位置づけ

`orchpipe/tuning.py` の倍音マッチが **A・E・C# を区別できない**という構造的な欠陥を持つ。
260829 の実データでこれが原因の誤検出が発生し、合奏3の開始境界が本来より **5分24秒
うしろ**に置かれ、チューニングと演奏の頭を切り落とした状態で配布してしまった。

この指示書はチューニング検出そのものを直す。ダイジェスト側の扱いは
`orchestra_recording_pipeline_digest_percussion.md` を参照のこと。

## 1. ブランチ

`main` から `feature/audio-quality` を作成する。**ラウドネス正規化
(`orchestra_recording_pipeline_loudness.md`)と同じブランチで進める。** 触るファイルが
重ならず(こちらは `tuning.py` と `segment.py`、あちらは `normalize.py` と `mix.py`)、
どちらも配布する音そのものを変えるため、まとめれば音源の再生成と試聴が1回で済む。
合否の見方は別物なので、まとめても切り分けられる。

## 2. 何が起きているか

現行の参照集合は「A のオクターブ5種 × 倍音1〜5次」である(`A_OCTAVES` × `A_HARMONICS`)。
倍音は音程比なので、次のように**別の音名と一致してしまう**。

| 倍音 | 音程比 | 音程 | 実際にマッチする音名 |
|---|---|---|---|
| 3次 | 3:1 | オクターブ+完全5度 | **E** |
| 5次 | 5:1 | 2オクターブ+長3度 | **C#** |

A・E・C# は A dur の三和音そのものであり、オーケストラの曲では常時鳴っている。

### 実測(260829、結合ファイル上の時刻)

現行実装が報告した5件のうち、本物は3件目までのクラスタだけである。

| 検出 | 長さ | 純度 | 実体 |
|---|---|---|---|
| 00:01:29 | 20s | 0.68 | 本物(1回のチューニングが3つに割れている) |
| 00:01:50 | 14s | 0.73 | 同上 |
| 00:02:06 | 27s | 0.59 | 同上 |
| 01:47:15 | 15s | 0.56 | **誤検出**。支配ピーク 1109Hz |
| 01:47:43 | 14s | 0.82 | **誤検出**。支配ピーク 1111Hz = C#6 |

1111Hz は A2(221Hz)の5倍音 1105Hz と 9 セントしか離れておらず、許容幅 ±60 セントに
余裕で収まる。A=442 での C#6 は 1113.8Hz であり、鳴っていたのは C# である。

さらに**本物の取りこぼし**もある。休憩1のあと 00:47:13 に実際のチューニングがあるが、
現行実装はこれを1件も報告していない。

### 倍音を使わない場合

支配ピークが A のオクターブ(A=442 の ±2 オクターブ)から ±60 セント以内にあり、
無音閾値超・純音性 0.20 以上、という条件だけで1秒ごとの一致率を出し、一致率 0.5 以上が
10秒以上続く区間を拾うと、260829 では次の3件**だけ**が得られた。

| 区間 | 長さ | 平均一致率 |
|---|---|---|
| 00:01:24 – 00:01:49 | 25秒 | 0.84 |
| 00:47:23 – 00:47:39 | 16秒 | 0.85 |
| 01:42:26 – 01:42:40 | 14秒 | 0.81 |

誤検出ゼロ、取りこぼしゼロである。

## 3. 直すこと

### 3-1. 参照集合から倍音を外す(主対応)

`A_HARMONICS` を使った参照周波数の拡張をやめ、**A のオクターブのみ**を参照する。

倍音マッチは「静かなオーボエ単独音では基音より上位倍音が支配的になる」という観察から
入れられたものだが(モジュール冒頭のコメント参照)、その意図は「基音が弱くてもよい」で
あって「基音がなくてよい」ではない。倍音だけを根拠に採用してよいことにした結果、
和音の構成音を拾うようになっている。

倍音を残す設計を選ぶ場合は、**A の基音帯にもエネルギーがあること**を必須条件として
加えること。倍音のみで条件を満たす窓は採用しない。どちらの設計でも構わないが、
§5 の検証を通ること。

### 3-2. 吹き直しの間隔をまたげるようにする

`DEFAULT_EVENT_GAP = 1.5` 秒では、チューニング中の短い途切れをまたげず、1回の
チューニングが複数のイベントに分割される(260829 冒頭が3件に割れているのがこれ)。

**チューニング中に数秒 A が途切れるのは正常な現象である。** 基準音 A を吹くオーボエ
奏者が、リードの状態などによって吹き直すために起きる。したがって:

- `DEFAULT_EVENT_GAP` を **10秒程度**まで広げ、近接するイベントを1回のチューニングと
  して束ねる。
- 束ねた結果の**最初の A** をそのチューニングの開始時刻とする。途切れのあとの2つ目を
  開始とすると、実際より数秒遅れて切ることになる。

260829 の合奏2では最初の A が 00:47:13、いったん切れて 00:47:17 から続く。正しい開始は
00:47:13 であり、00:47:17 を採ると4秒遅れる(実際にこの誤りを一度配布している)。

### 3-3. 純度と最短長の見直し

倍音を外すと一致率の水準が変わるため、`DEFAULT_MIN_PURITY`(現 0.55)と
`DEFAULT_MIN_DURATION`(現 12.0 秒)は §5 の検証を通る範囲で調整してよい。ただし
**選択性を落とさないこと**。6時間の素材から数件しか拾わない、という現在の性質は維持する。

## 4. 境界への反映

`segment.py` の `_select_tuning` は候補を「純度 × 長さ」のスコアで選んでいる。3-2 で
イベントを束ねるようにすれば、1回のチューニングが1イベントになるので、この選択は
「ブロックの粗い開始位置にもっとも近いイベント」で足りるはずである。スコアで選ぶ現行の
方式を残すか置き換えるかは実装者の判断でよい。

> **追記(260901)。** この指示書は「開始の5秒前をブロック開始とする規則
> (`TUNING_PRE_ROLL_S`)は変えないこと」と書いていたが、**2 秒に変更した。**
> 260829 の境界を人に聴いてもらったところ3ブロックとも「検出位置ちょうどまで
> 詰めてよい」という判定で、5 秒だと手前の音出しと無音が入るだけだった。
> ただしちょうどにはしない ― 同じ試聴で1件、A が想定より 1 秒ほど早く聞こえたと
> 報告があり、実測でも検出位置の 0.5 秒手前で A 成分比率が 0.279 まで上がる箇所が
> ある(検出位置では 0.105)。頭を倍音で前へ伸ばす `tuning_head_mask` は
> おおむね音の手前に開始を置くが、常にではない。2 秒はそのばらつきの吸収である。

## 5. 検証(必須)

### 5-1. 本物を全て検出すること

以下8点すべてを検出し、ブロック開始が**実際のチューニング開始と同じか、それより手前**に
なること(演奏・チューニングを絶対に削らない)。

| データセット | ブロック | チューニング開始(正解) |
|---|---|---|
| 260802 | 合奏1 | 00:10:01 |
| 260802 | 合奏2 | 01:40:43 |
| 260726 | 合奏1 | 00:08:37 |
| 260726 | 合奏2 | 01:14:31 |
| 260726 | 合奏3 | 02:24:49 |
| 260829 | 合奏1 | 00:01:22 |
| 260829 | 合奏2 | 00:47:13 |
| 260829 | 合奏3 | 01:42:20 |

260829 の3点は、A のオクターブのみによる1秒単位の一致率から求めた値である。

### 5-2. 誤検出を出さないこと

260829 の **01:47:15 と 01:47:43 を報告しないこと。** ここは C# を含む和音を演奏中の
箇所であり、これを拾うようだと 3-1 が効いていない。

3日ぶんの全長(260726・260802・260829 で計約9時間)を通して、検出されるイベントが
上記8件のクラスタ以外に出ないことを確認し、出た場合はその時刻の支配ピークの周波数を
報告すること。

### 5-3. 既存処理への非影響

`confirmed.json` は人手で確定した境界であり、この変更で自動的に書き換えてはならない。
既存のリグレッションチェック(sha256 の照合)を踏襲し、正規化・ミックス・エクスポート・
アップロードに影響がないことを確認する。

## 6. スコープ外

- **既に配布済みの 260726・260802・260829 の再生成・再アップロード・再通知。**
  実装と検証が終わった時点で報告し、再配布するかどうかは人間の判断を仰ぐこと。
- ダイジェスト側の扱い(`orchestra_recording_pipeline_digest_percussion.md`)。

## 7. 完了後の報告

- 8点の検証結果(すべて安全側に収まっているか)
- 誤検出の有無。出た場合はその周波数と音名
- 既存の `confirmed.json` と、新しい検出から導かれる境界との差分一覧
- リグレッションチェックの結果
